# 冉伟股票策略工作室

GitHub Pages 前端上传目录。入口为 `index.html`，PWA 图标在 `assets/icons/`。

双模式版：顶部切换标准模式与代理模式。代理模式包含文本/Word导入、逐条问答、确认看板、代码生成和分段解释。

前端配置了现有云函数地址；需同步部署新版云函数才能使用代理接口。DeepSeek密钥只放腾讯云函数环境变量，可选访问口令仅在页面内存使用。COS模板配置与发布细节见《双模式版本使用与部署说明.md》。
