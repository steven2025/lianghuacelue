# 代理模式 v1.2

同时更新前端和云函数。前端压缩包内容解压后覆盖原站点；云函数上传“双模式云函数_v1.2.zip”，入口仍为 index.main_handler，保留已有环境变量。云函数压缩包根目录包含 index.js 和依赖。

新增“选股＋剔除退出”用途，适用于股票池更新与剔除持仓退出，不自动买入。纯选股、信号提示、完整交易继续保留。股票池用途包含七项看板，只围绕选股及必要退出条件问答。

新增“已确认规则”记录区。保存后随项目保留，原样写入最终方案，参与服务签名并用于生成和修复。它是用户可编辑的独立规则记录；普通问答仍保存在聊天历史中，尚未实现自动提取每条回答的独立规则账本。后续修改已确认规则，请在该区修改并保存，避免与旧规则冲突。

本地文件“股票池构建策略0912_已确认.agent.json”包含原始文档和本次确认内容。更新程序后点击“导入已保存项目”，选择该文件，再点击“继续评估已有策略”。不必重答十大类、90日日均值、合格候选分母、同类递补和极端不足暂停规则。此文件未放入公开前端包，需从本地单独导入。

新增官方数据接口说明，本地模板和COS模板两种模式都会获得这些说明。未修改COS对象、云端配置或远程部署。要读取COS模板仍需AGENT_USE_COS=true；页面“连接与模板”可查看实际来源。

股票池不足保留旧池、禁止该次卖出等要求已加入生成协议；代码静态检查禁止明显买入和非允许下单接口。这些检查不等价于证明生成代码满足所有业务行为，最终需掘金运行验证。未调用真实DeepSeek或运行真实掘金策略。

仍需AGENT逐项确认PE/PB口径、非正值、风险警示范围、缺失数据、同分、未成交处理和首次持仓接入。通达信历史行业映射仍待验证来源；不能仅因存在通用基本面接口就视为所有历史数据齐备。
