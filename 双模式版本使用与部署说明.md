# 策略铸造台双模式版

本版保留原标准模式，新增独立代理模式。默认进入代理模式，顶部可随时切换；两种模式的策略、确认和代码状态互不覆盖。

## 代理模式使用

1. 粘贴文字或导入TXT、MD、DOCX。TXT/MD在浏览器读取（兼容UTF-8和GB18030），DOCX通过后端提取正文，最大2MB、正文40000字。导入后先显示在输入框，由用户点击“开始整理”才交给DeepSeek评估。
2. Agent复述策略，把内容整理到六项确认看板。每次只问一个业务问题，支持选项或自由回答。普通设置采用默认值时单独标记；模板或数据接口缺口单独显示，不要求用户填写函数名。
3. 点击看板“用文字修改”可提出调整。每次发送补充都会撤销旧方案确认，旧代码保留为历史版本。
4. 看板齐备后点击“确认方案并生成代码”。后端校验方案签名，检查生成代码的Python语法和基本入口/依赖。检查失败时自动尝试一次修复，仍失败则保留检查结果，不开放代码下载。
5. 右侧“代码与解释”逐段展示实际代码功能，展开可看对应代码。分段标记由后端转换为行号，不依赖模型估计行号。下载main.py后，在掘金配置GM_STRATEGY_ID和GM_TOKEN运行历史回测。
6. 将去除凭证的错误日志填入“回测报错了？”区域，可生成修复版本；保留最近6个版本，并可分别查看、下载。历史版本会明确标识，不代表当前策略。

代理项目自动保存在当前浏览器的独立localStorage键中。顶部“保存项目”下载代理项目JSON，包含原文、回答、方案及代码版本；不包含访问口令。导入或刷新恢复后，需重新评估方案才可再次生成或修复，已有代码仍可查看。确认签名有效期2小时。标准模式沿用原项目保存/恢复机制。

同一个浏览器内自动保存不等于云端备份；更换电脑或清理浏览器前，请下载项目。

## 前端发布

推荐整体上传`双模式前端_v1.0.zip`内文件到原站点根目录。

本次必须更新：

- index.html
- strategy_editor.html
- strategy_editor.js
- sw.js

本次新增：

- agent_core.js
- agent_workspace.js
- agent_workspace.css

旧的strategy_engine.js、strategy_ai.js、CSS和图标需保留。Service Worker缓存已更新到1.0。发布后如仍看到旧版，重新打开页面或强制刷新。

## 云函数发布

上传`双模式云函数_v1.0.zip`，ZIP根目录包括index.js、generation.js、agent.js、knowledge.js、docx.js、templates目录、package.json、package-lock.json和node_modules。函数入口仍是index.main_handler。依赖已更新，不能仅上传JS而继续使用旧node_modules。

运行时Node.js 18或以上；仍需DEEPSEEK_API_KEY、DEEPSEEK_MODEL及ALLOWED_ORIGINS，保留原COS_BUCKET、COS_REGION和执行角色。APP_ACCESS_TOKEN仍为可选访问保护，若设置需在代理模式“连接与模板”输入；手机也可进入此区域。

新增环境变量：

| 变量 | 含义 |
|---|---|
| AGENT_USE_COS | 设为true时使用COS模板目录；未设置时使用内置参考模板 |
| AGENT_TEMPLATE_PREFIX | 默认strategy-studio/templates/，末尾保留斜杠 |
| AGENT_SIGNING_KEY | 可选，建议设置独立随机密钥；未设置时后端使用DeepSeek密钥派生方案签名。此值不发给前端 |

使用COS模板时，执行角色需要新增该模板前缀的`name/cos:GetObject`权限。原报告归档前缀的`name/cos:PutObject`权限保留。前端不接触COS长期密钥，不直接读取私有桶。

## COS模板目录

在同一个私有桶放置：

```
strategy-studio/templates/index.json
strategy-studio/templates/fixed_pool.py
strategy-studio/templates/your_template.py
```

index.json示例：

```json
{
  "templates": [
    {
      "name": "固定股票池均线回测参考",
      "file": "fixed_pool.py",
      "tags": ["均线", "股票", "固定"],
      "validation": "已通过离线模拟测试，未在掘金实测"
    },
    {
      "name": "月度行业选股参考",
      "file": "your_template.py",
      "tags": ["行业", "选股", "每月", "财务"],
      "validation": "填写实际验证情况及适用SDK版本"
    }
  ]
}
```

file使用英文、数字、下划线或短横线的文件名，后缀py/md/txt。每个文件不超过120KB，目录最多40项；后端按原文与回答中的标签匹配最多3个模板，总长度不超过70000字。当前是标签匹配，不是向量检索。

模板应附上适用场景、接口来源、依赖数据和实际测试状态。移除真实Token、账户标识及密钥，凭证使用环境变量占位。可以上传带接口说明的Markdown补充资料。内置模板仅是固定股票池参考，不含完整历史行业映射和动态基本面选股接口；V9.7等复杂策略需要你补充相关模板/数据依据后再验证。Agent不会因为模板缺失而擅自替换策略规则。

## 当前验证与边界

已验证：标准模式回归、代理状态与签名、实际V9.7 DOCX正文提取、模拟COS读取/原报告归档、生成和修复协议，以及浏览器中的模式切换、问答、确认、解释、下载、版本与手机布局。新增依赖包含mammoth和@lezer/python，COS SDK升级为3.0.0，安装时审计为0项已知漏洞。

未验证：线上DeepSeek调用、你的私有COS实际权限、实际掘金回测。Python语法解析不等于运行验证，不检查所有数据权限、接口版本和交易行为。生成代码不在网页或云函数执行。

本版模型请求仍为同步模式，单次模型调用50秒超时，页面等待65秒；长策略可能超时或截断，显示错误并保留已记录问答。自动修复通过第二次独立请求进行，最多一次。取消页面等待不保证后端停止计费。尚未实现云端项目同步和异步长任务队列。

## 开发验证

```
node test_strategy_engine.js
node test_strategy_app.js
node test_ai_generation.js
node cloud_function/test_generation.js
node test_agent_mode.js
node cloud_function/test_agent_routes.js
```

`test_agent_preview.js`和`test_agent_browser.cjs`仅用于本地模拟浏览器测试，不放入生产发布包。
